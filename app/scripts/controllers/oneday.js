'use strict';

/**
 * @ngdoc function
 * @name travelApp.controller:OnedayCtrl
 * @description
 * # OnedayCtrl
 * Controller of the travelApp
 */
angular.module('travelApp')
  .controller('OnedayCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
